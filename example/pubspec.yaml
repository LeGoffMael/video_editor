name: example
description: A new Flutter project.

publish_to: "none" 
version: 1.0.0+1

environment:
  flutter: ">=3.0.0"
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  video_editor:
    path: ../

  helpers: ^1.1.4
  image_picker: ^0.8.5+3
  cupertino_icons: ^1.0.4
  video_player: ^2.4.5

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1